import { AuthForm } from "@/components/auth/auth-form"

export const metadata = { title: "Open an account — ForecastHub" }

export default function SignupPage() {
  return <AuthForm mode="signup" />
}
